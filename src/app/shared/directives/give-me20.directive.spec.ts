import { GiveMe20Directive } from './give-me20.directive';

describe('GiveMe20Directive', () => {
  it('should create an instance', () => {
    const directive = new GiveMe20Directive();
    expect(directive).toBeTruthy();
  });
});
